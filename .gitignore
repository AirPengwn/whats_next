# Local-only safety backups — never publish to the repo / GitHub Pages
.overhaul_backup/

# Claude Code local config / session data — keep out of the public repo
.claude/

# OS / editor cruft
.DS_Store
Thumbs.db

# Weekly-sweep routine scratch files (never publish)
_cloud_snapshot.json
_dedup_index.json

# Local-only preview dev server (never publish)
._preview_server.js

# All dot-underscore helpers/scratch (temp sweep scripts, preview server, etc.)
._*

# API keys for the weekly sweep — MUST never reach the public repo
._sweep_keys.json
*.key
secrets.json
